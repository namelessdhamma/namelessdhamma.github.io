[project]
name = "nd-notebooklm-oauth-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "notebooklm-py[mcp,headless,android]==0.8.2",
  "vercel>=0.5.0,<1",
]

[tool.vercel]
entrypoint = "app:app"
